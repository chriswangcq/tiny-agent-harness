// ─── Bash Tool Input ────────────────────────────────────────────────

import type { PtyAction } from "../terminal/types.js";

export type BashToolInput = BashPtyActionInput;

export type BashPtyActionInput = PtyAction;
